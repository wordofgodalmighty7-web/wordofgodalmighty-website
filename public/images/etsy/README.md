# Etsy product mockups

Product mockups live here as `product-1.png` through `product-7.png`.

To change images or labels, edit `src/lib/content.ts` (`etsyProducts` array).

Recommended: WebP or compressed JPG, roughly 800×800px or larger for sharp display.
